name: basicsr
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pip
  - python=3.8
  - pytorch 
  - torchvision<0.17
  - pytorch-cuda
  - pip:
      - anyio
      - basicsr
      - click
      - fastapi
      - h11
      - huey
      - idna
      - opencv-python
      - psutil
      - pydantic
      - requests
      - rq
      - sniffio
      - starlette
      - uvicorn